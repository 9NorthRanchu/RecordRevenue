# dependencies
# ไม่ใส่ / ท้าย เพราะ "node_modules/" จับได้เฉพาะโฟลเดอร์จริง
# ถ้ามี symlink ชื่อเดียวกันจะหลุดเข้า git ได้ (เคยเกิดมาแล้ว)
node_modules
frontend/node_modules/
venv/

# secrets & local config
.dev.vars
.wrangler/
*.env

# os / logs
.DS_Store
*.log
backup-before-trip-finance.sql
backend/db/live_backup.sql
backend/db/seed_historical_data.sql
