# PupPup Trip — สิ่งที่ requirement มี แต่ prototype ยังไม่มี

อ่านจาก `HANDOFF.md` + `TRIP_FINANCE_DESIGN.md` + โครงสร้าง backend ปัจจุบัน
เทียบกับ prototype 4 หน้า (`frontend/puppup-prototype/`)

mockup ที่ได้มา 4 ภาพ ครอบคลุมประมาณ **35%** ของสิ่งที่ระบบต้องมี — ที่เหลือคือหน้าจอ/flow ที่ยังไม่มีภาพต้นแบบ

---

## A. หน้าจอที่ยังไม่มีเลย

| # | หน้า | ทำไมต้องมี |
|---|---|---|
| A1 | **หน้ารวมทริป (Trip List)** | ทริปมี 3 สถานะ `active`=Ongoing · `planned`=Incoming · `closed`=Memory — ต้องมีที่ให้เลือกทริปก่อนเข้า 4 หน้านี้ · การ์ดทริปเดิมมีแบนเนอร์ + badge สถานะ + แถบงบประมาณอยู่แล้วใน `app.js` |
| A2 | **ปุ่ม/หน้า สร้างทริปใหม่** | ชื่อ · ปลายทาง · ช่วงวัน · สมาชิก · งบ · banner · รหัส guest |
| A3 | **โหมด Guest** | `POST /api/trips/guest` เข้าด้วย `trip_password` — ให้คนนอกครอบครัวดูทริปและบันทึกบิลได้ (บิลเข้าคิวรออนุมัติ) |

---

## B. Modal / flow ที่ปุ่มมีแล้วแต่ยังไม่มีหน้าจอ

| # | flow | endpoint ที่มีแล้ว |
|---|---|---|
| B1 | **เติมเงินเข้าทริป** — เลือกบัญชีต้นทาง + จำนวนบาท + สกุล + จำนวนตปท. + เรทคำนวณสด | `POST /api/trips/fund` ✅ |
| B2 | **สร้างกระเป๋าใหม่** — ชื่อ + สกุล + ยอดตั้งต้น + `exclude_on_close` | `POST /api/wallets` ✅ |
| B3 | **เพิ่ม/แก้บิล** — วันที่ · ผู้จ่าย · หมวด · กระเป๋า · จำนวน · **≈฿ สดข้างช่องกรอก** · จุดแวะ · รูปใบเสร็จ | `POST /api/trip-expenses` ✅ |
| B4 | **เครื่องคิดเลขแปลงเงินของทริป** — สองทาง ใช้เรทเฉลี่ยทริป | คำนวณ client-side จาก `avg_rate` ✅ |
| B5 | **ปิดทริป** — สรุปต่อคน/ต่อหมวด + ตรวจสมดุล + **เลือกจัดการเงินเหลือ (คืนบัญชี / ยกไปทริปหน้า)** + ยืนยัน `CLOSE` | `GET /api/trips/close-preview` · `POST /api/trips/close` ✅ |
| B6 | **เพิ่ม/แก้จุดแวะ + กิจกรรม + จุดย่อย** — ชื่อ EN/TH · เวลาเริ่ม–จบ · ไอคอน · สี · `parent_stop_id` | `POST /api/trip-stops` ✅ |
| B7 | **อนุมัติบิลของ guest** — คิวบิล `approved=0` | `POST /api/trip-expenses/approve` ✅ |
| B8 | **จัดการสมาชิกทริป + บทบาท** | `PUT /api/trips/members` ✅ (บทบาทเก็บใน JSON) |
| B9 | **จัดการเอกสารเดินทาง** | `/api/trip-documents` ✅ |
| B10 | **ตั้งงบประมาณ** + แถบใช้ไป/คงเหลือ | `Projects.total_budget` ✅ |
| B11 | **เลือกสกุลเงินหลัก** | 🆕 `Projects.base_currency` |
| B12 | **คลังไอคอน / อัปโหลดรูป** | ❌ ยังไม่ provision R2 |

---

## C. กฎทางธุรกิจที่ UI ต้องสะท้อน (ยังไม่มีใน prototype)

| # | กฎ | ผลต่อหน้าจอ |
|---|---|---|
| C1 | **Memory (`closed`) = อ่านอย่างเดียว** | ซ่อนปุ่มเพิ่ม/แก้/ลบทั้งหมด · FAB หาย · แสดง badge "ปิดแล้ว" |
| C2 | **Incoming (`planned`) = ยังไม่ผูกเงิน** | ซ่อนกระเป๋า/เติมเงิน เหลือแค่แผนเที่ยว + งบประมาณ |
| C3 | **ค่าบาทระหว่างทริป = ประมาณการ** ค่าจริงล็อกตอนปิด | ต้องมีป้าย "≈ ประมาณการ" ชัดเจนทุกที่ที่โชว์ ≈฿ ไม่งั้นผู้ใช้เข้าใจผิดว่าเป็นยอดสุดท้าย |
| C4 | **กระเป๋า `exclude_on_close=1` ที่ยังมีเงินเหลือ → ปิดทริปไม่ได้** | ต้องมี error state บอกว่าต้องปิดตัวเลือกนี้หรือใช้เงินให้หมดก่อน |
| C5 | **Reopen Memory** (P6) | ปุ่มเฉพาะ admin + ช่องระบุเหตุผล + คำเตือนว่าจะสร้าง reversal |
| C6 | **ปิดทริป = ลงบัญชีหลักถาวร** | ต้องมีหน้าสรุปให้ตรวจก่อนเสมอ ห้ามปิดด้วยปุ่มเดียว |

---

## D. หน้าที่มีแล้วแต่ยังไม่ครบ

| # | จุด | ขาดอะไร |
|---|---|---|
| D1 | หน้ากระเป๋าเงิน | โชว์การ์ดเดียว (JPY) — จริงๆ 1 ทริปมีได้หลายกระเป๋าหลายสกุล (THB/HKD/CNY/MOP/SGD ในทริปเดียว) |
| D2 | หน้าแผนเที่ยว — อากาศ | ต้องต่อ Open-Meteo + cache ใน `TripDayWeather` |
| D3 | หน้าแผนเที่ยว — หมุดบนแผนที่ | ตำแหน่งเก็บใน `Projects.route_data` ซึ่งยังว่าง · ต้องมี UI ลากวางหมุด |
| D4 | หน้าบิล — ชิปกรองหมวด | ยังไม่ทำงาน · ยังไม่มี "ดูทั้งหมด" / ค้นหา |
| D5 | หน้าบิล — สลับสกุล | ชิปสกุลอยู่หน้ากระเป๋าเงิน ยังไม่ผูกกับยอดรวมหน้าบิล |
| D6 | ทุกหน้า | ยังไม่มี loading / error / empty state (ยกเว้นวันที่ยังไม่ได้วางแผน ที่ทำแล้ว) |

---

## E. ที่ทำเพิ่มแล้วในรอบนี้ (นอกเหนือจาก mockup)

- **ทริปหลายวัน** — แถบเลือกวันแบบเลื่อนแนวนอน (วัน 1–7) พร้อมจุดสีประจำเมือง · เปลี่ยนวันแล้วหัวการ์ด อากาศ และรายการสถานที่เปลี่ยนตาม
- **วันที่ยังไม่ได้วางแผน** — empty state พร้อมปุ่มเพิ่มสถานที่
- **หลายกลุ่มสถานที่ในวันเดียว** — Asakusa → Shibuya โดยเส้นไทม์ไลน์ลากต่อเนื่องและเปลี่ยนสีตามย่าน
- **จุดย่อยในกิจกรรม (ชั้นที่ 4)** — Senso-ji → ไหว้พระ / ถ่ายรูป / ซื้อโอมาโมริ พร้อมเวลาแต่ละจุด
- **ช่วงเวลารวมของแต่ละย่าน** — chip `09:00–13:00` ข้างหัวข้อกลุ่ม

โครงสร้าง 4 ชั้นนี้ `TripStops.parent_stop_id` รองรับอยู่แล้ว:
`วัน → กลุ่มสถานที่ → กิจกรรม → จุดย่อย`

---

## สรุปปริมาณงานที่เหลือ

| หมวด | จำนวน | หมายเหตุ |
|---|---|---|
| หน้าจอใหม่ | 3 | A1–A3 |
| Modal/flow | 12 | B1–B12 — **10 ใน 12 มี endpoint พร้อมแล้ว** |
| กฎที่ต้องสะท้อนใน UI | 6 | C1–C6 |
| เติมของที่ขาดในหน้าเดิม | 6 | D1–D6 |
